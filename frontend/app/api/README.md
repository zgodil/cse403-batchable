# frontend/app/api
This directory contains the front-end's infrastructure for making well-typed and authorized API calls to both the mock and real back-end. Most files throughout the project should only import things from `/endpoints`, rather than this directory's direct contents.
