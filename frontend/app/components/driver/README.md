# frontend/app/components/driver
This directory contains all the components used on the driver-specific route page, `/route/:token`. This page allows for drivers to see their orders they need to deliver, mark them as delivered, and let the service know when they return to the restaurant. It also has confetti!
