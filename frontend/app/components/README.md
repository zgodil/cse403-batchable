# frontend/app/components
This directory contains all of the React components used in the front-end. Components which could only reasonably be used on a single page are nested within a like-named folder, e.g. `dashboard/`, `driver/`, `restaurant/`.
