# frontend/app/routes
This directory contains a `.tsx` file for each viewable URL route on the website. The names do not correspond to the URLs, but are instead associated with the proper URLs in `frontend/app/routes.ts`.
