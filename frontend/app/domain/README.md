# frontend/app/domain
This directory contains the generic `DomainObject` infrastructure used throughout the front-end. This allows for generic id'd objects of a certain form to be serialized and deserialized from JSON-esque representations in a highly type-safe and abstract way.
