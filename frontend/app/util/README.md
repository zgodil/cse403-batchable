# frontend/app/util
This directory contains utility and formatting functions for use throughout the front-end. In particular, `query.ts` contains the `useLoader()` hook, the preferred wrapper around async work (like API calls) for use within the project.
